#pragma once

#include <string_view>

namespace aiagent {

inline constexpr std::string_view version = "1.2.0";
inline constexpr int session_schema_version = 3;
inline constexpr int policy_schema_version = 1;

} // namespace aiagent
